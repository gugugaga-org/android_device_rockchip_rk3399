# Bluetooth vendor config placeholder.
#
# TPM312 uses a Realtek RTL8821CU USB combo: the Realtek vendor library
# (libbt-vendor-realtek.so) is selected at runtime from the detected WiFi
# chip type and reads /vendor/etc/bluetooth/rtkbt.conf instead of this file.
# The Rockchip common device.mk nevertheless requires this file to exist.
